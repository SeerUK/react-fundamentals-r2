// Lesson 15: Build a JSX Live Compiler
// This lesson was actually more of a recap, and just sort of interesting to watch. The things that
// happen in it are more to do with just trying stuff out that's already done. It also uses some
// stuff that would require index.html to be different, so fuqdat.

import * as React from "react";

export default class App extends React.Component<any, any> {
    public render() {
        return (
            <div>Hello, World!</div>
        );
    }
}
